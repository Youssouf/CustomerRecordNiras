@model IEnumerable<CustomerDataRecord.Models.Customer>
       
                     
           

@{
    ViewBag.Title = "Index";
}

<h2 class=" text-center"> List of Customers</h2>

<div id="detailsView">
    
</div>
<br />

<p>
    <span>
        <a href="@Url.Action("Create")"  class="btn btn-primary">

            <span class="glyphicon glyphicon-user"></span> Create customer</a>
       
    </span>
</p>


    <table class="table table-striped table-hover order-column table-responsive cell-border  table-bordered" id="myTable">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.FirstName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.LastName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Address)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.EmailAddress)
                </th>
              @*  <th>
        @Html.DisplayNameFor(model => model.Phone)
    </th>
    <th>
        @Html.DisplayNameFor(model => model.City)
    </th>
    <th>
        @Html.DisplayNameFor(model => model.Countrry)
    </th> *@
                <th>Action</th>
              @*  <th>View Company</th>*@
            </tr>
        </thead>
        <tbody>

            @foreach (var item in Model)
            {

                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.FirstName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.LastName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Address)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.EmailAddress)
                    </td>
                  @*  <td>
        @Html.DisplayFor(modelItem => item.Phone)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.City)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Countrry) *@ 

                        @*@Html.DropDownListFor(modelItem => item.Countrry, new SelectList(.Countrry,"CustomerID", "Countrry"))


    </td> *@
                    <td>
                        <span>
                            <a href="@Url.Action("Edit", new  { id = item.CustomerID })" class="btn btn-primary">

                                <span class="glyphicon glyphicon-pencil"></span> Edit
                            </a>

                        </span> |

                        @Ajax.ActionLink("View Companies", "FindCompanyForCustomerPartial", new { id = item.CustomerID }, new AjaxOptions
                   {
                       HttpMethod = "Get",
                       UpdateTargetId = "detailsView",
                       InsertionMode = InsertionMode.Replace
                   }, new { @class = "btn btn-primary" }
                   )|
                        @Ajax.ActionLink("Orders", "GetCustomerOrderList", new { id = item.CustomerID }, new AjaxOptions
                   {
                       HttpMethod = "Get",
                       UpdateTargetId = "detailsView",
                       InsertionMode = InsertionMode.Replace
                   }, new { @class = "btn"}
                   )|
                        @Html.ActionLink("Delete", "Delete", new { id = item.CustomerID }, new { @class = "btn btn-primary" })|
                       
                    </td>
                                     

                  @*  <td>
                      @Ajax.ActionLink("Cutomer Details", "CustomerDetails", new { id = item.CustomerID }, new AjaxOptions
    {
        HttpMethod = "Get",
        UpdateTargetId = "detailsView",
        InsertionMode = InsertionMode.Replace
    }, new { @class = "btn" }
    )|

                        @Html.ActionLink("View Company", "FindCompanyForCustomer", new { id = item.CustomerID }, new { @class = "btn btn-primary" })
                    </td>*@ 
                </tr>

            }

        </tbody>
    </table>


